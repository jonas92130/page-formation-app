import React from 'react'

interface Props {}

function NavBar(props: Props) {
  const {} = props

  return (
    <nav className="fixed left-0 right-0 top-0 flex w-full items-center justify-between"></nav>
  )
}

export default NavBar
